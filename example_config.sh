#!/bin/bash

# Update the two variables and then rename and move the file to ./config/config.sh

#use `which aws` to find the path
AWS="/path/to/aws"
s3_bucket_name="BUCKETNAME"
backup_folder_name="mastodon-backup"
